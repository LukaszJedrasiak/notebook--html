It is recommended to use `<button>` every time you need to make button - even if you want to make i.e. clickable image.

You can disable all default styles of the button with the following rule:

```css
all: unset;
```

